{
  "name": "chuchu-bot",
  "version": "2.0.0",
  "private": true,
  "description": "CHUCHU BOT v2 - Institutional-grade Real-time AI Crypto Intelligence & Trading Platform",
  "scripts": {
    "build": "pnpm --recursive run build",
    "dev": "pnpm --parallel --filter ./packages/* run dev",
    "test": "pnpm --recursive run test",
    "lint": "pnpm --recursive run lint"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.2",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}
